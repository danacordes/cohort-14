/** Entity slices stored in embedding_record.entity_type — align with blueprint + Data model. */
export const EMBEDDING_ENTITY_TICKET = 'Ticket';
export const EMBEDDING_ENTITY_KB_ARTICLE = 'KBArticle';
